# Wall-E 新硬件主板设计说明（方案 A：双层堆叠模块插接底板）

> 2026-08-10。配套文档：`hardware/新硬件BOM与尺寸.md`（BOM 与尺寸，尺寸已经人工复核更正）。
> **进度（2026-08-14）**：主板原理图已定稿并落地 `hardware/walle-mainboard/`（KiCad 10 工程，`generate_schematic.py` 生成/校验，ERC 0 error）；板框按用户定稿改为 **80 × 63mm**（见 §4，原建议 90×72 作废）。PCB 布局未开始。
> 本文件是方案 A 底板的**原理图级设计输入**：所有连接器逐脚定义 + 电源网络 + 布局建议，可直接照此在 EDA 工具中画原理图/PCB。
> 结构：**单块 PCB、模块双面插接堆叠**——顶层 ESP32-S3 与 LU9685 并排，底层 DC-DC 降压 + TB6612 + PCM5102 + PAM8406；板上只有连接器、分压电阻等少量元件，全部直插，2 层板即可。12V 仅 2 个输入焊盘，开关/保险丝不上板。

---

## 1. 连接器 / 插座定义（逐脚）

### J1 — ESP32-S3 主控插座（2×22P 排母，2.54mm）

两排 22P 沿板子**长边**（57.15mm 边）左右分布：单排首尾针距 **53.34mm**（21×2.54，人工实测），两排间距 **25.40mm**（厂商图纸值，画封装前卡尺复核）。

排针顺序按 `YD-ESP32-S3资料` 尺寸图与实物照片（天线端 → USB 端）。**加粗**为底板实际使用的网络，其余脚位排母照焊但不接线（NC）。

| 左排（天线端→USB端） | 网络 | 右排（天线端→USB端） | 网络 |
|---|---|---|---|
| 3V3 | **3V3** | GND | **GND** |
| 3V3 | 3V3 | TX(43) | **UART0_TX** → J-DBG |
| RST | NC | RX(44) | **UART0_RX** → J-DBG |
| 4 | **MIC_WS** | 1 | **BAT_ADC**（分压中点） |
| 5 | **MIC_SCK** | 2 | **4G_TX**（可选） |
| 6 | **MIC_SD** | 42 | **TFT_BLK** |
| 7 | **DAC_DIN** | 41 | **BTN_RST** |
| 15 | **DAC_BCK** | 40 | **TFT_DC** |
| 16 | **DAC_LCK** | 39 | **BTN_VDN** |
| 17 | **MOT_AIN1** | 38 | **BTN_VUP** |
| 18 | **MOT_AIN2** | 37 | NC |
| 8 | NC | 36 | NC |
| 3 | **4G_RX**（可选） | 35 | NC |
| 46 | NC | 0 | **BTN_BOOT** |
| 9 | **CAM_TX**（主控发） | 45 | **TFT_RES** |
| 10 | **CAM_RX**（主控收） | 48 | NC |
| 11 | **MOT_PWMB** | 47 | **TFT_SDA** |
| 12 | **MOT_BIN1** | 21 | **I2C_SCL** |
| 13 | **MOT_BIN2** | 20 | **I2C_SDA** |
| 14 | **TFT_SCL** | 19 | **MOT_PWMA** |
| 5V | **5V**（逻辑轨） | GND | GND |
| GND | **GND** | GND | GND |

> ⚠️ 板载两个 USB-C：一个原生 USB（19/20，**已被占用**，仅烧录可用）、一个板载 USB 转串口（接 43/44，**调试日志用这个口**）。底板上仍建议留 J-DBG 备用。
> ⚠️ 天线区 4.6mm 必须悬空出板边或设禁布区，底板铺铜避让。

### J2 — LU9685 舵机驱动插座（2×7P 排母，2.54mm）

模块左右两排 7P 弯针，丝印相同。两排同网络可并联（增加载流）。

| 模块丝印 | 底板网络 | 说明 |
|---|---|---|
| V+ | **5V_SERVO**（大电流轨） | 两排 V+ 都接，分摊舵机电流 |
| DVCC | **5V** | 逻辑电 |
| SDA | **I2C_SDA** (GPIO20) | |
| SCL | **I2C_SCL** (GPIO21) | |
| RXD | NC | 串口模式不用 |
| TXD | NC | |
| GND | **GND** | |

> 模块上另有 2P 接线柱（V+ 舵机电源入口，厂商标 3.08mm 间距）。**建议降压模块 5V 输出除走底板铜皮到 7P 的 V+ 外，再备一根短线直接进该接线柱**（舵机峰值 4–6A，排针单脚 ~3A）。
> 16 路舵机座在模块本体上，布局时舵机座一侧朝板边，方便插拔延长线。通道 9 = 照明灯（`LIGHT_PWM_CHANNEL`）。
> I2C 地址默认 0x40（丝印 0x80 为 8 位格式），与固件一致，A0–A4 跳线不动。

### J3 — TB6612FNG 电机驱动插座（2×8P 排母）

模块丝印为准（不同批次位置略有差异，**按信号名对插**）：

| 模块丝印 | 底板网络 | 模块丝印 | 底板网络 |
|---|---|---|---|
| VM | **12V** | PWMA | **MOT_PWMA** (GPIO19) |
| VCC | **5V** | AIN1 | **MOT_AIN1** (GPIO17) |
| GND | GND | AIN2 | **MOT_AIN2** (GPIO18) |
| AO1/AO2 | → **J-MOTA** | PWMB | **MOT_PWMB** (GPIO11) |
| BO1/BO2 | → **J-MOTB** | BIN1 | **MOT_BIN1** (GPIO12) |
| | | BIN2 | **MOT_BIN2** (GPIO13) |
| | | STBY | **5V**（常开，`MOTOR_STBY_WIRED=0`） |

### J4 — GY-PCM5102 DAC 插座（排母）

| 模块丝印 | 底板网络 |
|---|---|
| VCC/VIN | **5V** |
| GND | GND |
| BCK | **DAC_BCK** (GPIO15) |
| DIN | **DAC_DIN** (GPIO7) |
| LCK/LRCK | **DAC_LCK** (GPIO16) |
| L/R 输出 | → **J5 PAM8406 输入**（板上直连，双声道并联进单声道或各带一喇叭，按实物模块输出形式） |

### J5 — PAM8406 功放插座（排针/焊盘）

| 引脚 | 网络 |
|---|---|
| 5V/VDD | **5V** |
| GND | GND |
| LIN / RIN | ← PCM5102 L/R |
| L+ L− | → **J-SPK1**（XHB 2P，喇叭 1） |
| R+ R− | → **J-SPK2**（XHB 2P，喇叭 2） |

### J6 — DC-DC 降压模块（10–36V→5V）焊盘/端子

实测 42.00 × 24.00mm，输入 2P 在左侧（间距 18.00mm），输出 2P 在右侧（间距 18.00mm）：

| 引脚 | 网络 |
|---|---|
| IN+ / IN− | **12V**（开关后） / GND |
| OUT+ / OUT− | **5V 总轨** / GND |

> ✅ 选型定稿（2026-08）：现选模块持续 **15A**，覆盖舵机峰值 4–6A + 4G 发射瞬态 2A + 逻辑 ~2A（极端同发 ~10A 仍有 ≥5A 余量），**支持舵机**。另有一款持续 **8A** 的同型模块：**不支持舵机**（舵机联动 + 4G 发射同发即超限，封闭机身降额后更紧张），禁止用于本板舵机轨，仅可用于无舵机的纯逻辑供电场景。

### 板边连接器（对外线束）

| 位号 | 连接器 | 逐脚定义 |
|---|---|---|
| J-BAT | **焊盘 ×2**（Ø2mm，间距 5.08） | 1=**BAT+**（12V 入）2=GND；**开关与保险丝不上板**，在板外电池线上自行串接（面板 8mm 带灯开关串 BAT+） |
| J-MOTA / J-MOTB | XH 2.54 2P ×2 | 左电机 A / 右电机 B（← TB6612 AO/BO）；**左右板边同高度镜像**（MOT-A 左 / MOT-B 右，与 ESP32 同一水平区） |
| J-PANEL | FPC 22P 1.0mm **下接翻盖座**（**左板边** → 副板/面板板） | 喇叭/麦/按键/1.3″屏全部经此口到副板；**22 针逐脚定义见下表**；线材 FPC 22P 1.0mm **反向** 30cm（BOM #29） |
| J-CAM | XHB 2.54 4P（**右板边**） | 1=**5V** 2=GND 3=**CAM_TX**(9)→CAM RX(14) 4=**CAM_RX**(10)←CAM TX(21)（交叉在背板完成，主板侧直出；丝印注明） |
| J-DBG | 2.54 焊盘 3P（**底层**） | 1=**UART0_TX**(43) 2=**UART0_RX**(44) 3=GND |
| J-4G | 2.54 排母 **2×6P**（**顶边左**，经线束接 4G 核心板） | **针序 2026-08 实测定稿**：核心板左右两排 6P，各排**从下往上为脚 1→6**，主板侧 J-4GA/J-4GB 与之同序直通。**左排** 1=**5V**（4G 专用，独立走线+储能） 2=GND 3=TXD→**4G_RX**(GPIO3) 4=RXD←**4G_TX**(GPIO2) 5=**EN**（电源使能，板上 10k 上拉 5V 默认开机） 6=BAT（3.7V，不用→NC）；**右排** 1=**5V**（与左排并联，分摊 2A 瞬态） 2=GND 3=USB_DP（不用→NC） 4=USB_DN（不用→NC） 5=**NET**（网络指示，可选板载 LED） 6=BOT（悬空→NC）；线束：**2×6P 双头排母 ~30cm**，两端同序直通（左排对左排）；两排排距按实物复核（布局阶段） |

**J-PANEL 22 针逐脚定义**（喇叭= PAM8406 差分输出共地；按键一脚信号 + 一脚 GND，GPIO 内部上拉低有效）：

| 脚 | 网络 | 副板去向 | 脚 | 网络 | 副板去向 |
|---|---|---|---|---|---|
| 1 | **5V** | 面板供电（备用） | 12 | **BTN_BOOT** (GPIO0) | 按键 BOOT |
| 2 | **3V3** | 麦/屏 3V3 | 13 | **BTN_VUP** (GPIO38) | 音量+ |
| 3 | **GND** | 共地 | 14 | **BTN_VDN** (GPIO39) | 音量− |
| 4 | **GND** | 共地 | 15 | **BTN_RST** (GPIO41) | 长按重启 |
| 5 | **SPK_L+** | 左喇叭 + | 16 | **TFT_SCL** (GPIO14) | 屏 SCL |
| 6 | **SPK_L−** | 左喇叭 − | 17 | **TFT_SDA** (GPIO47) | 屏 SDA |
| 7 | **SPK_R+** | 右喇叭 + | 18 | **TFT_RES** (GPIO45) | 屏 RES |
| 8 | **SPK_R−** | 右喇叭 − | 19 | **TFT_DC** (GPIO40) | 屏 DC |
| 9 | **MIC_SCK** (GPIO5) | 麦 SCK | 20 | **TFT_BLK** (GPIO42) | 屏 BL |
| 10 | **MIC_WS** (GPIO4) | 麦 WS | 21 | **GND** | 屏/麦 地 |
| 11 | **MIC_SD** (GPIO6) | 麦 SD | 22 | **3V3** | 屏供电 |

> 反向 FPC 线 1↔22 镜像：两端座子 1 脚丝印相对，装配时按丝印对插即可，网络定义不变。

## 2. 板上少量元件

| 位号 | 器件 | 说明 |
|---|---|---|
| D1 | 10A10 二极管（或 P-MOSFET 理想二极管） | 防反接，**可选**——不用则短接焊盘 |
| R1/R2 | 100k + 33k（1%） | 电池分压：**12V 输入焊盘**→ R1 → GPIO1 → R2 → GND；12.6V → 3.13V |
| D2 + R3 | 3mm LED + 1k | 5V 电源指示 |
| C1/C2 | 470µF/25V 电解 + 100nF | 5V 轨入口储能（降压模块近旁） |
| C4G | 470µF/10V 电解 | J-4G 接口旁，4G 发射 2A 瞬态储能（掉压即掉网） |
| R_EN | 10k 上拉电阻 | J-4G 的 EN 脚 → 5V，默认开机（4G 模组电源使能） |

> 以上小元件全部放**底层**（与降压模块同面），不占顶层空间。

## 3. 电源网络与设计规则

| 网络 | 电流 | 走线要求（1oz 铜） |
|---|---|---|
| 12V（输入焊盘→降压 IN / TB6612 VM） | ≤5A | ≥2.5mm，短直 |
| 5V_SERVO（降压 OUT → LU9685 V+） | 峰值 4–6A | **铺铜 ≥4mm** + 模块 2P 端子直连备份线 |
| 5V（逻辑：主控/CAM/DAC/功放） | ≤2A | ≥1.5mm |
| 5V_4G（J-4G → ML307R-DL） | 发射瞬态 2A | ≥1.5mm 独立走线，接口旁 470µF 储能；掉压即掉网 |
| 3V3（主控板载 LDO → MIC/TFT） | ≤0.3A | 普通信号线宽 |
| GND | — | **双面铺铜**，多打过孔；降压、LU9685、TB6612 的 GND 星型汇合到电池入口 |

- 信号线宽 ≥0.25mm（10mil），间距 ≥0.2mm，2 层板 1.6mm 厚，全直插。
- 舵机大电流回路不经过主控 5V 脚；主控由逻辑 5V 供电（接 J1 的 5V 脚，经其板载 LDO 出 3V3）。

## 4. 布局与板框建议

> 📐 **等比例布局示意图：`hardware/主板布局示意图.html`**（浏览器打开，6px=1mm，顶/底两面视图，模块按实测尺寸绘制）。

- **结构：单块 PCB、模块双面插接堆叠**。顶层（朝上）= ESP32-S3 与 LU9685 并排；底层（朝下）= DC-DC 降压 + TB6612 + PCM5102 + PAM8406 + 小元件（C1/D1/R1R2/LED/J-DBG 焊盘）。
- **板框定稿：80 × 63mm**（2026-08 用户定稿 63×80，取代原建议 90×72；原理图与最小板框已落地 `hardware/walle-mainboard/`），四角 M3 安装孔（Ø3.2，孔心距边 4mm：(4,4)/(76,4)/(4,59)/(76,59)）；安装铜柱高度 **≥15mm**（底层模块最高约 11mm）。
- 双面插接可行性：排针+排母总高 ~8.5mm，背面针头露头仅 ~2.4mm，**上下层模块 footprint 允许重叠**（模块背面无高器件即可）。
- 布局原则：
  - **顶层两模块长边一律沿 80mm（X）边方向、上下并排**：ESP32-S3（57.15×27.94）+ LU9685（64.01×27.18）总高 55.12mm ≤ 63mm；⚠️ LU9685 长 64.01mm > 63mm 短边，**不可横放**。ESP32 的 USB-C 端朝板边、前方留 ≥15mm 插线空间；天线区在模块端部、板内设**禁布条**（宽 ≥4.6mm，无铜无件，推荐 ≥6mm）。
  - LU9685 16 路舵机座朝**下板边**出线；其顶部 V+ 2P 端子用短线直连底层降压输出（5V 舵机轨）。
  - 底层：降压与 PAM8406 放上排，PCM5102 与 TB6612 放下排，小元件集中右下；PCM5102 ↔ PAM8406 模拟走线经板层最短连接。
  - 对外连接器全在**顶层左右板边**：**MOT-A（左）↔ MOT-B（右）同高镜像**（与 ESP32 同一水平区）；**左板边** = MOT-A + **J-PANEL（FPC22P → 副板）**；**右板边** = MOT-B + CAM；**顶边** = 12V 输入焊盘居中 + **J-4G（4G 模组）靠左**；下边 = 舵机线出口（不放连接器）；J-DBG 3 焊盘在底层。**面板件（喇叭/麦/按键/1.3″屏）不设单列接口**——全部经 J-PANEL 的 22P 软排线到副板（见 §6）。⚠️ 上述板边分配基于原 90×72 板框，**80×63 下的最终排布以布局阶段实排为准**。
  - **4G 模组（远程）**：ML307R-DL **mini 核心板（AT 固件，24.2×20.0）** 经 **2×6P 双头排母线束 ~30cm** 接 J-4G（针序 2026-08 实测定稿，见 §1），核心板放机体空位，吃 5V（自带稳压 + SIM 座）；J-4G 的 5V 独立走线 + 接口旁 470µF（C4G），UART 交叉（模块 TXD→GPIO3、RXD←GPIO2）；**EN 高电平通电 → R_EN 10k 上拉 5V 默认开机**；BAT/USB_DP/DN 不用，BOT 悬空。
  - **12V 输入仅 2 个焊盘**（Ø2mm，间距 5.08）；开关与保险丝不上板，在板外电池线上自行串接（面板 8mm 带灯开关串 BAT+）。

```
侧视堆叠示意：

 顶层 │ ESP32-S3（57.15×27.94，USB→板边，天线板内禁布） ║ LU9685（64.01×27.18，舵机座↓）
 ════╪══════════════════ 主板 80×63 ══════════════════
 底层 │ DC-DC 42×24 │ PAM8406 23×24 │ PCM5102 32×17 │ TB6612 18×21
      └── M3 铜柱 ≥15mm 到机体底板
```

## 5. 摄像头背板（头部小板）

> 参考实物照片：`hardware/另一套硬件-摄像头背板.png`（第三方背板：两列排母插 CAM 模块 + 顶部两个排线座接眼屏）。
> 布局示意：`主板布局示意图.html` 下方第三视图。CAM 模组与两块眼屏都插在这块板上，经 XHB 4P 50cm 线连主板 J-CAM。

- **板框：40 × 55mm**，M2.5 安装孔 ×4。
- **CAM 模块**：艾尔赛 **ESP32CAM_V1.1（303ESPCAM01）**，27×40mm，资料在 `hardware/ESP32S3_CAM/`（原理图/规格/测试程序）。
- **CAM 模块插座**：2×8P 排母（2.54），**两排间距 22.86mm**，针序已按厂商原理图定稿：

  | JP1（8P） | EN · U0TXD(43) · U0RXD(44) · 3V3 · GND · BOOT(0) · IO2 · 3V3 |
  |---|---|
  | **JP2（8P）** | **5V · GND · IO46 · IO45 · IO42 · IO41 · IO21 · IO14** |

  ⚠️ 该模块**仅引出 GPIO 0/2/14/21/41/42/45/46**（+UART0 43/44）——这就是眼屏和 UART 只能用这些脚的原因，没有别的选择。
- **眼屏接口 ×2**：按眼屏转接板引出形式（多为 8P FFC 排线座或 2.54 排针，**实物实测后定稿**）。每屏实际使用信号：

  | 信号 | 左屏 | 右屏 |
  |---|---|---|
  | SCL / SDA(MOSI) / DC / RST | GPIO42 / 45 / 41 / 46（两眼共享，均取自 JP2） | 同左（共享） |
  | CS（片选，厂商丝印 L/R） | GPIO2（JP1） | GPIO0（JP1，与 BOOT 同脚；CS 空闲高，不影响启动） |
  | VDD / GND | 3V3 / GND | 3V3 / GND |

  **接口针序按眼屏转接板实物实测**，未用针悬空；GPIO0/45/46 为 strapping 引脚（CS 空闲高、SCL 空闲低，已实机验证可正常启动）。
- **SD 卡（结论：不用）**：板载 TF 卡槽按原理图走 SDMMC——CLK=GPIO39 / CMD=GPIO40 / D0=GPIO41 / D1=GPIO42（D2/D3 仅上拉）。**不可用**：39/40 未在排针引出，41/42 又与眼屏 DC/SCK 冲突，SPI 模式也因 D3 未接而无 CS。拍照/录像走 HTTP 从 PSRAM 取，不落本地 SD。此前"SD 与摄像头共用 GPIO8/9"的说法源于固件里 XIAO 旧预设，对本模块不成立（`docs/PCB_REQUIREMENTS.md` §2.3 已更正）。
- **J-PWR 4P（XHB）**：来自主板 J-CAM——5V / GND / 主控9→CAM RX(14) / 主控10←CAM TX(21)。**UART 交叉在背板上完成**（主板侧 J-CAM 按 9/10 直出）。
- **J-DBG 焊盘 ×3**：CAM 日志（JP1 的 U0TXD/U0RXD/GND），联调用。

## 6. 副板（面板板）

> 参考实物照片：`hardware/另一套硬件-副板.png`。面板件（喇叭/麦/按键/1.3″屏）集中在这块板上，经 **FPC 22P 1.0mm 反向 30cm** 连主板 J-PANEL。布局示意见 `主板布局示意图.html`（副板视图）。

- **板框：70 × 35mm**，M2.5 安装孔 ×4，装于机体前面板内侧。
- **J-PANEL 对端座**：FPC 22P 1.0mm 下接翻盖座（BOM #28）×1，接主板 J-PANEL 的 22 针（反向线按丝印对插，网络定义不变）。
- **喇叭**：SPK_L± / SPK_R± 直焊焊盘（2828 腔体喇叭 ×2，PAM8406 差分输出共地）。
- **麦克风**：INMP441 2×3P 接口（VDD=3V3、GND、SD、SCK、WS、L/R→GND；针序以实物丝印为准）。
- **按键 ×4**：CHA PB13 焊盘，各一脚接信号（BOOT/音量+/音量−/重启）、一脚接 GND（GPIO 内部上拉，低有效）。
- **1.3″ 状态屏**：7P 排母（GND/3V3/SCL/SDA/RES/DC/BLK），插 ST7789 模块。
- 22 针逐脚定义见 §1 J-PANEL 表。

## 7. 打样前检查清单

1. 按 `新硬件BOM与尺寸.md` §8 清单逐项复核（CAM 板型、各模块实测尺寸、降压模块电流）。
1b. 摄像头背板专项：CAM 模块=艾尔赛 ESP32CAM_V1.1，插座 2×8P 间距 22.86mm（JP1/JP2 针序已定稿）；眼屏 CS=2/0（模块唯一可用 IO）实机验证；SD 卡不可用。
2. J1 排母 2×22P：单排首尾 **53.34mm**（21×2.54）、排间距 **25.40mm**、孔径 Ø0.9–1.0mm；LU9685 两排 7P 的排距按实物卡尺（尺寸图 64.01×27.18，排针在短边两侧）。
3. TB6612 / PCM5102 / PAM8406 三种模块**排针 pin 序以实物丝印为准**，本说明只定义信号对应关系——画封装前先拿到实物量一遍。
4. 丝印：所有连接器标注位号 + 逐脚网络名（照 §1 表），J-CAM 注明交叉。
5. 眼屏 FPC、圆屏、摄像头全部在 CAM 模组侧，**主板无关**，不要为它们留接口。
